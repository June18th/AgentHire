# 02-✅求职派本地开发说明手册

本文的内容相对轻松，适合准备动手调试、扩展求职派源码的小伙伴。

将从以下部分进行展开，降低大家的学习成本

1. 项目结构说明
2. 不同开发环境的说明
3. 关键功能生效的代码路径
4. 项目约定

## 一、项目源码获取

源码申请戳这个链接，一次申请，可以获取星球所有项目源码：https://paicoding.com/ai-agent-projects-guide

## 二、源码工程结构说明

进入项目源码，大家会看到下面几个目录

```bash
├── app                # 后端工程项目的入口（Web管理、数据采集、支付等）
├── core               # 共享基础模块（智能体内核、消息路由、模型调度）
├── channels/          # IM渠道接入（微信/钉钉/飞书）
│   ├── wechat-clawbot
│   ├── dingding
│   └── feishu
├── providers/         # AI模型提供商适配
│   ├── zhipu
│   ├── openai
│   ├── anthropic
│   └── ali
├── agents/            # 业务智能体
│   ├── identity-collector-agent   # 身份采集
│   ├── job-fetch-agent            # 职位获取
│   └── job-recommend-agent        # 职位推荐
├── plugins/           # 插件扩展
│   ├── playwright     # 网页抓取
│   ├── job-library    # 职位库
│   └── plan-notebook  # 规划笔记
├── workspace/         # 运行时工作空间（数据库、用户档案、对话记录）
├── docs               # 项目相关的静态资源、文档说明
├── ui-react           # 前端工程项目的入口
└── readme.md          # 帮助文档
```

整体采用 Maven 多模块架构，模块依赖关系如下：

![](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618084040.png)

新业务智能体放在 `agents/` 下，不要放在 `app/` 里（除非它纯粹属于 Web 管理/数据采集流水线）。

### 1.后端项目结构

后端使用的技术栈为：`Spring Boot 4.0.5 + Java 21 + Spring AI 2.0.0-M4 + LangGraph4J 1.6.0 + Spring JPA + JobRunr + JWT`

![](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618085109.png)

①、采用标准的后端MVC分层架构

- Controller： 定义数据访问端点（即各种http接口）
- Service: 提供服务实现
- Dao: 数据库访问

②、关键目录的功能划分如下所示：

```bash
├── agents/      # LangGraph4J 多智能体工作流（任务分类→数据采集→清洗→发布）
├── components/  # 通用组件
├── configs/     # 全局字典
├── constants/   # 常量定义
├── gather/      # AI大模型相关，任务采集
├── llm/         # LLM调用监控与管理
├── oc/          # 正式职位数据服务、MCP Server端点
├── openapi/     # 开放平台接口(主要是与技术派进行账号互通的实现)
├── user/        # 用户相关服务，包含登录、用户管理、会员充值等
├── util/        # 一些工具类
├── web/         # SpringMVC相关，提供controller入口，登录鉴权，全局管理等
```

③、业务领域划分：

- agents: LangGraph4J 多智能体工作流（TaskClassify → TaskGather → DraftWasher → DraftPublish）
- configs: 全局字典
- gather: AI大模型 + 数据抓取解析任务（GatherAiAgent、OfferGatherService）
- llm: LLM调用监控与管理
- oc: 校招职位信息、MCP Server端点
- user: 用户 + 会员充值

④、辅助工具层：

- openapi：开放平台接口(主要是与技术派进行账号互通的实现)
- components：通用组建，如异步调度、业务异常、全局上下文、雪花算法生成ID策略
- constants: 常量定义，包括不同业务领域相关的枚举、常量等
- util：工具类集合，通常一个类就是一个工具，包含常见的JsonUtil, IpUtil, DateUtil, SessionUtil等

⑤、WEB端点访问层主要是提供web服务相关的合集

接下来以user这个业务领域为示例，对其中的包结果定义进行说明:

```bash
├── convert                                  # 定义类型转换器的包结构
│   ├── CouponConvert.java
│   ├── RechargeConvert.java
│   └── UserConvert.java
├── dao                                      # dao下为与数据库交互的实现
│   ├── entity                               # entity下面定义的是数据库实体对象
│   │   ├── CouponEntity.java
│   │   ├── RechargeEntity.java
│   │   ├── UserEntity.java
│   │   └── UserInterestEntity.java
│   └── repository                           # repository下为具体的数据库交互定义/实现，求职派采用的JPA进行数据库交互
│       ├── CouponRepository.java
│       ├── RechargeRepository.java
│       ├── UserInterestRepository.java
│       └── UserRepository.java
├── helper                                   # 当前业务领域专属的辅助工具集
│   ├── SessionHelper.java
│   ├── UserRandomGenHelper.java
│   ├── WxLoginProperties.java
│   └── WxLoginQrGenIntegration.java
├── model                                    # model下定义业务领域对象，通常是代码内部使用，不与外部系统交互（即常说的BO对象）
│   ├── PayCallbackBo.java
│   ├── PrePayInfoResBo.java
│   ├── RechargeCnt.java
│   ├── ThirdPayOrderReqBo.java
│   └── UserInterestBo.java
├── package-info.java
└── service                                  # 具体的Service服务实现层（这里没有定义接口，只有实现，主要是为了简化工程结构，这和工程项目中面向接口编程有一定差异）
    ├── CouponService.java
    ├── LoginService.java
    ├── RechargeService.java
    ├── UserInterestService.java
    ├── UserService.java
    └── pay
        ├── ThirdPayHandler.java
        ├── ThirdPayIntegrationApi.java
        └── wx
```

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753753505637-87e6a137-8c25-49cb-9537-fefdd640f4f8.png)

然后再介绍一下 web 下的工程目录

```bash
├── config                         # 配置文件，初始化相关
│   ├── SiteConfig.java
│   ├── WebConfig.java
│   ├── WebFileConfig.java
│   ├── WxPayConfig.java
│   └── init                       # 使用MySql类关系型数据库作为数据存储的场景时，这里实现库表初始化
├── controller                     # 用户访问端点（即接口定义）
│   ├── admin                      # 管理员相关接口
│   └── front                      # 前台用户相关接口
├── hook                           # WEB服务相关钩子，用于增强web相关能力（如鉴权、记录访问信息、在线人数统计等）
│   ├── extend                     # 定义全局异常处理、全局返回结果封装
│   ├── filter                     # 封装请求用户上下文，记录请求日志
│   ├── interceptor                # 用户权限管理
│   └── listener                   # 在线用户统计
└── model                          # 请求/返回实体定义
    ├── req                        # 请求实体定义
    ├── res                        # 返回结果实体定义
    └── wx                         # 微信交互实体定义
```

### 2.前端项目结构

```bash
├── app/             # 业务页面
├──── admin/         # 后台管理页面
├──── internal/      # 前台页，内推tab页
├──── internship/    # 前台页，实习tab页
├──── job/           # 前台oc详情页
├──── user/          # 前台用户详情页
├──── page.tsx       # 主页
├── components/      # 通用UI组件
├── hooks/           # 抽象的工具方法
├── lib/             # http访问、字典配置管理工具
├── styles/          # 全局样式
```

前端采用的框架是 react + nextjs + paxios，`typescript`作为编程语言， 使用`tailwind`实现样式布局。

## 三、多环境说明

### 1.Maven多环境配置

后端应用，借助maven的profile实现多环境配置，具体的定义在 `app/pom.xml`配置文件中

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753754561748-897ceb0b-ce39-4887-9fd4-2afbcc374fbb.png)

三个环境，对应的配置文件分别为上图中左边 `resources-env` 下面的三个不同的目录

- dev: 开发环境
- test: 测试环境
- prod: 生产环境

### 2.环境切换

本地开发时，如何进行环境切换呢？

- idea右侧边，点击 M 图标
- 展开Profiles
- 勾选需要切换环境前面的复选框
- （说明：native 和 nativeTest是SpringBoot提供的环境配置，我们没有使用）

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753754831753-8c9c4071-3828-4b55-b202-b3d8a886e3a2.png)

### 3.环境差异性说明

三个不同的环境，核心区别在于配置

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753755025811-cec35825-f32e-44d8-b5f1-40fdc6e7183d.png)

从上面的对照可以明显看出：

- dev: 使用的是 h2database ，数据库文件放在 `workspace/datas/` 目录下
- test: 使用的是 mysql，需要指定你自己的数据库配置信息

:::warning
知识点：maven的profile多环境，还支持不同环境引入不同的依赖

:::

## 四、本地开发配置

`.env` 环境变量、智谱 API Key、H2 数据库拷贝等基础配置已在上一篇 [✅求职派本地开发环境搭建指南](https://paicoding.com/jobclaw-dev-environment-setup) 中详细介绍，这里不再重复。

本篇聚焦上一篇未涉及的进阶配置。

### 1.MySql数据库配置

对于已经安装了 MySQL 的小伙伴，强烈推荐使用 MySQL 作为数据存储层；H2 虽然简单，但稳定性和可靠性，以及功能的完备性相对来说要差一点；如果你本机已经安装了 MySQL，那么在本地开发时，请切换 `test` 开发环境。

#### 1.切换 test 开发环境

![灰灰截图](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753756151579-d16a0e60-93f1-4e64-a168-221ddcd6231a.png)

![二哥截图](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618084638.png)

#### 在 .env 中配置 MySQL 连接信息

test 环境的 `application-dal.yaml` 已经通过环境变量占位符引用 `.env`，无需直接修改 yaml 文件。只需在 `.env` 中设置以下配置即可：

```bash
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USERNAME=root
DATABASE_PASSWORD=你的MySQL密码
JOBCLAW_DATABASE_NAME=jobclaw
```

:::tip
H2 和 MySQL 的数据库名完全独立。

H2 用的是文件名（如 `jobclaw-my`），MySQL 用的是数据库名（如 `jobclaw`）。

切换 Maven Profile 即可切换数据源，`.env` 中的 `JOBCLAW_DATABASE_NAME` 在两个环境下分别指向不同的东西，按需修改即可。
:::

#### 启动项目，确定库表初始化情况

:::info
求职派与技术派一样，提供了自动创建库、表，基于 Liquibase 实现了数据库版本管理，因此你完全不需要手动创建表。

:::

第一次切到 test 环境时，建议先在项目根目录执行一次完整安装：

```bash
./mvnw -U -Ptest -DskipTests clean install
```

![](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618092523.png)

二期拆了多个模块，`app` 会依赖 `plugins/plan-notebook`、`core`、`agents/*` 等本地模块。

直接启动 `app` 时，如果这些模块还没有安装到本地 Maven 仓库，可能会出现类似下面的错误：

```text
Could not resolve dependencies for project com.git.hui.jobclaw:app
com.git.hui.jobclaw.plugins:plan-notebook:jar:0.0.1-SNAPSHOT was not found
```

安装完成后，再启动后端：

```bash
./mvnw -Ptest -pl app spring-boot:run
```

![](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618092718.png)

正常启动时，可以重点看这几类日志：

```text
Copying 3 resources from src/main/resources-env/test to target/classes
[Dotenv] .env file found at: .../.env, loading properties...
[Dotenv] Loading property: JOBCLAW_DATABASE_NAME = jobclaw
Liquibase: Update has been successful.
Tomcat started on port 8087 (http) with context path '/'
```

启动完毕之后，刷新你的数据库连接工具，比如说Navicat，默认的库名为 `jobclaw`。

![](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618092836.png)

首次初始化完成后，MySQL 中应该能看到业务表和 Liquibase 版本表，例如：

```text
common_dict
databasechangelog
databasechangeloglock
draft_oc
gather_task
global_env_config
llm_invocation
llm_request
mcp_client
oc_info
recharge_coupon
user_info
user_interest
user_recharge
```

另外还会有 JobRunr 自动创建的任务调度表，例如 `jobrunr_jobs`、`jobrunr_metadata` 等。

如果你启动后只能看到 `jobrunr_*` 表，看不到 `global_env_config`、`user_info`、`oc_info` 这类业务表，说明业务 changelog 没有执行，需要优先检查 `app/pom.xml` 是否引入了 `spring-boot-starter-liquibase`，以及 `spring.liquibase.enabled` 是否为 `true`。

如果要在命令行确认库表数量，可以执行：

```bash
mysql -h localhost -P 3306 -u root -p -e "SELECT table_name FROM information_schema.tables WHERE table_schema='jobclaw' ORDER BY table_name;"
```

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753756414887-e9810819-d9ec-404b-a58a-fb1b60e263b9.png)

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753756452004-d9d1d416-17da-4517-a192-ffac932d125a.png)


假如你已经切换到了 test 的 Maven 配置，但实际执行的时候仍然是通过 dev，那说明 Maven 的缓存被污染了。重新执行一下下面的命令应该就好了。

```bash
./mvnw clean
```

可以在 OcDataSourceInitializer 的 autoInitDatabase 方法中打个断点，看一下输出，是否和你在 test 的 application-dal.yaml 文件中的 MySQL 配置匹配。

![](https://cdn.nlark.com/yuque/0/2025/png/12564477/1759995047655-7dd9a890-0fc0-401e-a1ef-bcd5d17ad6c5.png)

### 2.初始化账号管理

初始的数据中，默认提供了两个用户，一个管理员，一个普通用户。

为了解决本地开发时，微信公众号的回调无法直接打到本地应用导致无法登录的情况（当然可以做内网穿透），求职派提供了开发环境快速登录能力。

#### 本地开发登录（无需配置）

**本地开发时不需要任何额外配置**，前端会自动检测当前访问地址是否为 `localhost`（或 `127.0.0.1`）。

如果是，登录弹窗会自动切换为"本地登录"模式，直接显示「普通用户登录」和「管理员登录」两个按钮，点击即可一键登录。

操作步骤：

1. 启动后端，访问 `http://localhost:8087`
2. 点击右上角「登录」按钮
3. 弹窗标题显示"本地登录"，直接选择身份即可

![](https://cdn.paicoding.com/stutymore/02-✅求职派本地开发说明手册-20260618093900.png)

:::tip
原理：前端 `AppLayout.tsx` 中判断 `localLoginEnabled = localHost || isDevEnv`，本地访问时 `localHost` 为 true，自动启用。

登录时调用后端 `/api/wx/dev/login` 接口，传入 `type=user` 或 `type=admin` 区分身份。
:::

#### 非 localhost 环境启用 Dev Login

如果你的开发环境不是通过 localhost 访问的（比如用了 IP 或域名），需要手动开启：

1. 先用管理员身份登录（首次可通过 localhost 登录后再切换访问方式）
2. 进入后台管理：点击右上角头像 → 选择「管理后台」→ 左侧菜单选择「字典管理」
3. 找到 `全局-站点配置`，将 `env` 对应的 `value` 设置为 `dev`

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753756729055-8ca8d4b6-2497-4855-9cba-fe8c8ee45d97.png)

设置后，前端无论从哪个地址访问都会显示 Dev Login 按钮。

### 3.支付配置

求职派是提供了完整的微信支付功能，对于需要在本地进行支付测试验证的小伙伴

- 需要将证书拷贝到项目的 resources 目录下，默认是放在 cert 目录
- 修改`application-pay.yml`中的支付信息

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753756964670-33fb1f79-3d7a-44a6-8b36-08e510985253.png)

在测试的时候，我们可能需要小金额进行验证，因此需要调整充值金额，此时进入求职派后台管理 - 字典管理，修改金额

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757072551-99e28be1-7af6-42bf-83e3-0c3a0445459e.png)

## 五、核心功能模块

### 1.用户认证与权限管理

1. 支持关注微信公众号，发送验证码登录的方式，便捷又安全，降低了用户注册登录的门槛。
2. 基于 jwt（JSON Web Token）的用户登录鉴权，实现了无状态的身份验证。同时，自定义的权限管理模块，能精准控制不同用户的操作权限，保障系统数据安全。

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758640134-7482dc20-3e38-48c1-b367-1e1b60298228.png)

### 2.基于大模型的校招信息采集Agent

借助 SpringAI 实现大模型的数据采集，这是求职派的一大技术亮点。

1. 提交采集任务后，系统会在任务表中插入一条记录，然后后台异步执行采集任务，不影响前端用户的操作。
2. 支持多种数据输入方式，包括文本、图片、csv 文件、excel 文件、http 网页、http 链接等，能全面覆盖各类校招信息的来源，满足多样化的采集需求。

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758693255-4de32eda-0a6c-41f8-b9dc-bf5e0a33456d.png)

### 3.异步AI调度任务机制

我们设计了一个任务池的数据模型，将用户提交的采集任务先放到任务池，然后进行异步的任务调度执行，从而减少大模型响应慢的问题

采集数据后，支持数据清洗、同步到正式的oc库，采用采集数据与正式数据分离的思想，避免污染业务库。

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758722020-e4bc5ea7-c923-4261-8c11-b30fe52a807e.png)

### 4.会员充值

集成微信支付实现会员充值，为项目的商业化运营提供了支持，开发者可基于此拓展更多增值服务。

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758755059-91a4ac5a-f055-46f6-8cad-d7a019a720cc.png)

### 5.全局字典

包结构：com.git.hui.jobclaw.configs

- 定义求职派的各种字典数据、业务数据

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758435010-b9b7a778-7cd0-4fdb-950a-6407ab3b503d.png)

### 6.多渠道智能体系统

求职派的核心亮点之一 — 基于 `core` 模块实现的多智能体消息管道：

- **IM 渠道接入**：微信（ClawBot）、钉钉、飞书，每个渠道独立模块，通过 `Channel.adaptToReceive()` 统一消息格式
- **消息路由**：`MsgRouter` 统一处理消息流转 — 身份采集 → 系统命令 → 意图识别 → 智能体路由
- **意图分类**：`IntentClassifier` 关键词 + LLM 复合分类，将用户消息路由到正确的业务智能体
- **业务智能体**：身份采集（identity-collector）、职位获取（job-fetch）、职位推荐（job-recommend）

### 7.MCP Server

求职派集成了 Spring AI 的 MCP Server 能力，对外暴露职位查询等工具，可被其他 AI 应用（如 Claude Desktop）直接调用：

- 协议配置在 `application.yml` 中的 `spring.ai.mcp.server` 下
- 支持 SSE 和 Streamable HTTP 两种传输方式
- 配置开关：`.env` 中的 `AGENT_MCP_CLIENT_ENABLED`

### 8.用户身份三层体系

求职派为每个用户维护三层画像，存储在 `workspace/users/{userId}/` 下：

- **Soul**（`soul/`）：个性特征、职业志向（AI 提取）
- **Identity**（`user/`）：技能、教育、工作经历
- **Info**（`info/`）：基础人口统计信息

由 `IIdentityAgent.triggerToCollectIdentity()` 在 `MsgRouter` 中自动触发，当用户画像不完整时主动采集。

### 9.LangGraph4J 多智能体工作流

`app/agents/` 下基于 LangGraph4J 实现了数据处理的多阶段工作流：

```
TaskClassify → TaskGather → DraftWasher → DraftPublish
```

用于校招信息从提交到发布的全流程自动化。

## 六、关键功能实现说明

### 1.权限管理

对于用户，我们区分了三种类型，普通 + 会员 + 管理员，不同的用户类型有不同的访问权限；

如何设置一个接口的访问权限呢?

- 在类、方法上添加注解：@Permission(role = UserRoleEnum.ADMIN)

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757222999-85cd3dfb-504a-4629-b9d3-912770cfb6d4.png)

- 权限校验逻辑实现：com.git.hui.jobclaw.web.hook.interceptor.PermissionCheckInterceptor

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757425512-7a895427-150c-40b5-8537-b521a6a06046.png)

### 2.公众号登录

微信公众号登录的逻辑沿用的是技术派的这一套思路，有兴趣的小伙伴看：[✅技术派微信公众号自动登录（👍强烈推荐）](https://www.yuque.com/itwanger/az7yww/xbrhgpae2ii4mawd)[✅技术派微信扫码登录实现（👍强烈推荐）](https://www.yuque.com/itwanger/az7yww/ecdm9y6tsgbvvrw5)

关键流程：

- 用户打开登录弹窗，发起sse请求，返回验证码
- 用户在微信公众号输入验证码
- 微信公众号将验证码转发给求职派服务器
- 求职派根据验证码，找到登录的用户，实现自动登录跳转

核心实现入口：

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757620677-e9393bef-cdc5-49f7-b2de-433b98293892.png)

### 3.全局异常管理

借助 RestControllerAdvice 实现

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757743588-b8b044ad-c9f1-4185-b593-c200cd611639.png)

### 4.统一定义返回结果

借助 ResponseBodyAdvice 实现

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757797898-3d14a797-54a7-4019-bf4f-ff0a70829cf9.png)

### 5.微信支付

去支付：RechargeController

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757839875-46e0627c-4c93-404e-b4ce-5255d5af09ad.png)

支付的核心实现

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757871579-aab13819-04a9-4a01-a72e-dae3cd6fedca.png)

### 6.SpringAI相关

用户提交任务，SpringAI执行任务的相关实现，在 gather 业务领域下

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753757937183-271df2ba-104b-4487-ba46-641c26a3be3d.png)

### 7.任务池

用户提交的任务附件，如csv/excel/图片，是保存到本地的，数据库中存储的是引用地址而不是文件内容

本地文件的读写由 LocalStorageHelper 实现

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758058951-fbdf8f14-08b7-4466-af95-6d5b16e5f93b.png)

本地文件存储路径通过 `.env` 配置：

```bash
JOBCLAW_IMG_ABS_TMP_PATH=/tmp/storage/       # 本地文件存储根路径
JOBCLAW_IMG_WEB_IMG_PATH=/oc/img/             # Web 访问路径
JOBCLAW_IMG_CDN_HOST=http://localhost:8087    # CDN / 本地访问域名
```

对应 `application.yml` 中 `jobclaw.img` 下的配置项。

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758112783-7537dc45-1071-4fc0-8b1c-112095011415.png)

任务池调度采用两种方式：

1. **Spring @Scheduled**：`OfferGatherService` 中通过 `@Scheduled(cron = "0 0/5 * * * ?")` 定时扫描待处理任务
2. **JobRunr**：`core` 模块中基于 JobRunr 实现更灵活的后台任务调度（Dashboard 在 8099 端口），适用于智能体的异步任务管理

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758286103-3a4ef7cf-db14-481a-852c-351397c0e06f.png)

### 8.在线用户统计

基于weblistener实现，核心逻辑在 OnlineUserCountListener

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758334439-dee62768-a429-4953-b761-ea9b33770a11.png)

### 9.用户上下文

在用户访问的链路上，都可以通过 `ReqInfoContext.getReqInfo()`获取全局上下文，包括访问用户、请求链路等信息，由 `ReqRecordFilter` 提供上下文维护

![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753758918128-7f86a29d-d24b-49bd-b58e-79be9cb17642.png)

## 七、约定

自我感觉没有太多潜规则，基本上都是java开发这一套规则，如果在实际体验中，有发现与自己理解不一致的、或者与业界主流玩法不同的，欢迎评论指出
