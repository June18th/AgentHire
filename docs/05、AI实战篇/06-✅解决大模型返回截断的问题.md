这一篇将以真实遇到的大模型交互问题出发，来综合演示下如何灵活运用 SpringAI 的多轮对话、提示词模板、结构化输出、工具调用、返回结果处理等知识点，来解决实际的应用问题。

# 一、问题表现
在 [✅求职派如何实现Function Calling](https://www.yuque.com/itwanger/yyt72l/adluns1gyfzndgnv)  这一篇，提到了大模型返回结果被截断的问题。当时没有深入说明，接下来审查一下，这里的问题到底是啥。


场景复现：求职派提取文本时，方法签名上已经明确写了数据太长，会导致异常。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753843808722-c062a390-ecf4-4b12-9bb2-164a8236b11b.png)

那我们准备一波数据，看看实际的效果。

```latex
公司名称	公司类型	工作地点	招聘类型	招聘对象	岗位(我该投啥岗位?)	投递进度	更新时间	投递截止	相关链接	招聘公告	内推码	备注
中国科学院杭州医学研究所科学实验中心	事业单位	浙江	秋招	2025年毕业生
细胞/透明化制样与显微成像技术工程、生物信息分析师、动物仪器平台技术员、饲养员
未投递	2025-07-30	招满为止	投递	公告	-	要求硕士
北京联合大学	事业单位	北京	秋招	2025年毕业生
教师
未投递	2025-07-30	招满为止	投递	公告	-	要求硕士
国家国际发展合作署直属事业单位	事业单位	北京	秋招	2025年毕业生
英语翻译
未投递	2025-07-30	招满为止	投递	公告	-	要求硕士
白山市事业单位	事业单位	白云市	秋招	2024和2025年毕业生
建筑消防、工程监督、供水员、工程管理、财务会计、司法数据分析、环境监测、环境监管、监察执法、综合岗位等
未投递	2025-07-30	招满为止	投递	公告	-	-
蚂蚁集团	民企	北京 上海杭州	秋招提前批	2025和2026年毕业生
助理 研究员、多模态大模型研发、算法工程师、AI基础系统研究工程师、算法工程师、具身智能算法工程师、多模态大模型研发;
未投递	2025-07-30	招满为止	投递	公告	-	-
睿远基金权益研究部	民企	上海	秋招	2026年毕业生
医药行业研究员
未投递	2025-07-30	招满为止	投递	公告	-	要求硕士
剧星传媒	民企	上海北京 广州深圳杭州天津 厦门合肥郑州	秋招	2026年毕业生
品牌策划、客户执行、销售、数据分析、达人策划、达人执行、运营、短视频编导，
未投递	2025-07-30	招满为止	投递	公告	-	-
云南建投集团	央国企	云南	秋招	2024和2025年毕业生
施工管理岗、人力资源岗、预核算员、材料员、财务管理、综合管理岗、造价管理岗等
未投递	2025-07-30	招满为止	投递	公告	-	-
紫光国芯	央国企	西安	秋招	2026年毕业生
芯片架构岗、ASIC数字设计岗、ASIC数字验证岗、ASIC数字后端岗、存储器数字设计岗、混合电路设计岗、模拟电路设计岗、设计分析与验证岗、产品开发岗、可测性设计岗
未投递	2025-07-30	招满为止	投递	公告	-	-
天津出版传媒集团有限公司	央国企	天津	秋招	2025年毕业生
书刊编辑、编辑
未投递	2025-07-30	招满为止	投递	公告	-	-
江苏高科技投资集团有限公司	央国企	江苏	秋招	2024和2025年毕业生
投资、法务、风控等岗位
未投递	2025-07-30	招满为止	投递	公告	-	-
美团	民企	北京 上海深圳成都广州香港利雅得拜圣保罗等	秋招	2026年毕业生
技术、产品、商业分析、运营、金融、零售职能、市场营销、设计、销售、客服、支持类岗位;
未投递	2025-07-30	招满为止	投递	公告	-	-
东风汽车研发总院	央国企	武汉	秋招	2025年毕业生
计算机数学类、电气自动化类、电子信息类机械车辆类、能源动力类、材料化学类;
未投递	2025-07-30	招满为止	投递	公告	-	-
商汤科技	民企	北京 上海深圳香港杭州 武汉	秋招	2025和2026年毕业生
AI先锋顶尖人才、AI领航员应届生:算法研究类、工程开发类、项目管理类、职能类岗位;
未投递	2025-07-30	招满为止	投递	公告	-	-
壹思唯国际教育	民企	北京 深圳	秋招提前批	2026年毕业生
教学教研、市场营销、教辅、职能;
未投递	2025-07-30	招满为止	投递	公告	-	-
极客未来·人力资源管培生	民企	成都、重庆、乐山、自贡、宜宾、遂宁、泸州、内江、南充、德阳等	秋招	2025和2026年毕业生
人力资源管培生
未投递	2025-07-30	招满为止	投递	公告	-	-
山西证券	央国企	北京 上海深圳厦门西安 大连宁波石家庄成都阳泉忻州晋城等	秋招	2025年毕业生
投资助理岗、交易岗、产品岗、销售岗、主观研究岗、量化研究岗、公募基金业务岗、私募孵化岗、利率投资助理岗等
未投递	2025-07-30	招满为止	投递	公告	-	-
国机重装	央国企	德阳市	秋招	2026年毕业生
机械类,金属材料类,冶金类,电气类,能源动力类,智能制造类等理工科专业
未投递	2025-07-29	招满为止	投递	公告	-	要求硕士
星辉游戏	民企	广州	秋招	2026年毕业生
策划类、技术类、美术类、综合类;
未投递	2025-07-29	招满为止	投递	公告	-	-
深圳新东方	民企	深圳	秋招提前批	2026年毕业生
少儿素养班课教师、初中素养班课教师、高中班课教师、高中一对一教师;
未投递	2025-07-29	招满为止	投递	公告	-	-
原力灵机	民企	北京成都	秋招	2026年毕业生
具身智能大模型算法研究员、具身智能强化学习算法研究员、机器人系统算法工程师、具身智能传感器工程师等
未投递	2025-07-29	招满为止	投递	公告	-	-
芯原股份	民企	上海成都南京 海口广州	秋招	2026年毕业生
硬件设计类、软件开发类;
未投递	2025-07-29	招满为止	投递	公告	-	-
卓驭	民企	深圳 上海	秋招	2026年毕业生
软件类、算法类、运维类、嵌入式类、安全类、测试类、机械电气类、系统工程类、非研发类岗位;
未投递	2025-07-29	招满为止	投递	公告	-	-
辽宁省地质勘探矿业集团有限责任公司	央国企	辽宁	秋招	2025年毕业生
专业技术（地质、采矿、造价、物探、水工环、地下工程、遥感等）
未投递	2025-07-29	招满为止	投递	公告	-	-
国投生物制造创新研究院有限公司	央国企	无锡	秋招	2026年毕业生
研究员（高通量平台方向、发酵/分离纯化方向、功能评价与应用开发方向）
未投递	2025-07-29	招满为止	投递	公告	-	要求硕士
福建投资集团	央国企	福建	秋招	2025年毕业生
综合管理部行政文秘岗、资金财务部会计岗、资金财务部出纳岗等
未投递	2025-07-29	招满为止	投递	公告	-	-
京东方-先锋京英计划岗位扩容	民企	北京 烟台合肥	秋招提前批	2026年毕业生
钙钛矿工程开发工程师、包装设计研究员、器件仿真工程师、BIPV产品开发工程师、钙钛研发工程师、电路开发研究员等
未投递	2025-07-29	招满为止	投递	公告	-	-
杰理科技	民企	珠海 深圳	秋招	2026年毕业生
算法类、IC设计类、应用类、综合职能类，
未投递	2025-07-29	招满为止	投递	公告	-	-
浪潮集团	央国企	济南全国其他	秋招提前批	2026年毕业生
核心研发类、产品研发类、技术支持类、市场类、财经法务类、职能管理类;
未投递	2025-07-29	招满为止	投递	公告	-	-
立行教育集团	民企	济南北京广州等	秋招	2026年毕业生
小初高各科教师、高中竞赛教练、职能管理岗、教育咨询师;
未投递	2025-07-29	招满为止	投递	公告	-	-
```

在管理端提交。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753843933377-f17cf2dd-62b8-44d3-9b36-aac87006c63d.png)


执行之后，会发现控制台报错了，提示返回结果无法转换为目标对象；从最后的的日志可以看出，大模型返回的不是一个完整的json字符串，被截断了。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753844221460-ab4715bc-2ed7-4456-a444-8e91a0f13912.png)


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753844251756-ff5f8a2c-971b-409c-a4a9-8f520cf48afc.png)


# 二、优化方案
说明大模型的返回结果有自己的长度上限；当解析结果超出预期后，就会出现截断的问题；那改成异步的流式方案，能解决这个问题吗？（还未测试，等我验证后再来解答）


既然一次返回不了，那就尝试少给一些数据给他分析，但对应的问题又来了，应该从什么地方进行截断呢?


+ 用户输入的文本，不确定是否会从中间截断、导致大模型解析不了完整的数据；
+ 其次就是，不确定大模型的返回极限在哪里，万一我拆分之后大模型还是截断怎么办？
+ 对于输入是图片或者网页链接的场景，即便我想截断，好像也无能为力


因此最好考虑让大模型多次返回，接着上次没有返回的地方继续。基于这个方案，我们就需要维护对话上下文。

## 1.多轮对话实现大模型继续解析
接下来我们直接使用 `ChatModel`进行多轮对话，并将每轮对话保存下来，拼接之后再映射为目标对象。


多轮对话可以使用`SpringAI`的`ChatMemory`来维护，最开始是系统提示词。

```java
// 创建 memory 实例，保存上下文
ChatMemory chatMemory = MessageWindowChatMemory.builder().maxMessages(10).build();
String chatId = RandomUtil.randomString(6);

SystemMessage systemMessage = new SystemMessage(OcChatModelApi.GATHER_SYSTEM_PROMPT);
chatMemory.add(chatId, systemMessage);
```


对于第一次对话，用户输入的内容为需要采集的信息； 对于非第一次对话，需要给大模型的提示是 “`让他继续返回上次未回答完的内容`”

```java
 while (true) {
    log.info("{}#第{}次大模型数据解析", chatId, cnt + 1);
    UserMessage msg;
    if (cnt == 0) {
        UserMessage.Builder builder = UserMessage.builder()
                .text(new PromptTemplate("{text}.{format}")
                        .render(Map.of("text", text, "format", gatherResConverter.getFormat()))
                );
        if (media != null) {
            builder.media(media);
        }
        msg = builder.build();
    } else {
        msg = new UserMessage("你之前返回的结果不完整，继续返回剩余的内容");
    }
    chatMemory.add(chatId, msg);

     ... 省略后面的实现
 }
```


因为我们使用的是ChatModel进行对话，因此结构化返回需要我们主动定义，通过`BeanOutputConverter`和 提示词模板 来实现。

:::success
知识点一：使用提示词模板 `PromptTemplate.render()`来实现文本替换

知识点二：结构化输出的本质是生成返回的`json schema`，然后作为对话内容传给大模型

+ 结构化输出：`gatherResConverter = new BeanOutputConverter<>(new ParameterizedTypeReference<>() {});`

:::


使用 Function Call 为大模型赋能

```java
// 工具
ChatOptions chatOptions = ToolCallingChatOptions.builder()
        .model(modelPair.getSecond())
        // 注册给大模型回调的工具
        .toolCallbacks(ToolCallbacks.from(new CrawlerTools()))
        .build();
```


将大模型每次返回的结果，先存在来，最后做统一解析（为避免死循环，可以加一个最大的对话次数限制）


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753845636357-5d905373-9fd1-4b16-b866-d85b8a09aad0.png)

接下来我们看一下完整的多轮对话实现。

```java
    /**
     * 针对大模型响应结果截断的场景，进行多轮对话，尝试获取完整的返回
     * 实现原理：基于 chatModel, 借助 ChatMemory 自动实现多轮对话，
     */
    private List<GatherOcDraftBo> autoContinueChat(GatherModelEnum model, Media media, String text) {
        // 创建 memory 实例，保存上下文
        ChatMemory chatMemory = MessageWindowChatMemory.builder().maxMessages(10).build();
        String chatId = RandomUtil.randomString(6);

        SystemMessage systemMessage = new SystemMessage(OcChatModelApi.GATHER_SYSTEM_PROMPT);
        chatMemory.add(chatId, systemMessage);

        // 选择具体交互的大模型
        Pair<ChatModel, String> modelPair = ocAiModelContext.model(ModelSelectReq.of(model, media == null ? GatherModelTypeEnum.CHAT_MODEL : GatherModelTypeEnum.IMAGE_MODEL));

        List<String> itemList = new ArrayList<>();
        StringBuilder remain = new StringBuilder();
        int cnt = 0;
        while (true) {
            log.info("{}#第{}次大模型数据解析", chatId, cnt + 1);
            UserMessage msg;
            if (cnt == 0) {
                UserMessage.Builder builder = UserMessage.builder()
                        .text(new PromptTemplate("{text}.{format}")
                                .render(Map.of("text", text, "format", gatherResConverter.getFormat()))
                        );
                if (media != null) {
                    builder.media(media);
                }
                msg = builder.build();
            } else {
                msg = new UserMessage("你之前返回的结果不完整，继续返回剩余的内容");
            }
            chatMemory.add(chatId, msg);

            // 工具
            ChatOptions chatOptions = ToolCallingChatOptions.builder()
                    .model(modelPair.getSecond())
                    // 注册给大模型回调的工具
                    .toolCallbacks(ToolCallbacks.from(new CrawlerTools()))
                    .build();
            try {
                Prompt query = new Prompt(chatMemory.get(chatId), chatOptions);
                if (log.isDebugEnabled()) {
                    // 一行显示日志
                    log.debug("{}#req: {}", chatId, StringUtils.replaceChars(query.toString(), "\n", ""));
                }
                ChatResponse response = modelPair.getFirst().call(query);
                AssistantMessage assistantMessage = response.getResult().getOutput();
                if (log.isDebugEnabled()) {
                    // 一行显示和日志
                    log.debug("{}#res: {}", chatId, StringUtils.replaceChars(assistantMessage.toString(), "\n", ""));
                }

                chatMemory.add(chatId, assistantMessage);
                cnt += 1;

                String outText = assistantMessage.getText().trim();
                itemList.addAll(GatherResFormat.extract(remain, outText));
                if (outText.endsWith("```") || cnt >= 10) {
                    // 做一个次数限制，避免死循环的调用大模型
                    log.info("{}#经过{}论对话，完成大模型调用", chatId, cnt);
                    break;
                }
                if (cnt > 1 && outText.startsWith("```json")) {
                    int jsonBeginIndex = outText.indexOf("[");
                    if (jsonBeginIndex > 0 && jsonBeginIndex < 15) {
                        // 表示大模型又重新返回了完整的数据，为了避免大模型总是重复解析，我们直接退出循环
                        log.info("{}#大模型重复返回完整解析数据，主动退出多轮对话", chatId);
                        break;
                    }
                }
            } catch (Exception e) {
                // 避免因为多次调用模型出现异常，导致前面获取的数据被丢掉，我们直接跳出来，将已经解析的结果保存下来
                log.error("{}#gather error", chatId, e);
                break;
            }
        }

        if (itemList.isEmpty()) {
            return List.of();
        }

        return itemList.stream().map(this::toBo).filter(Objects::nonNull).collect(Collectors.toList());
    }
```


## 2.多轮对话返回结果处理
多轮对话的实现起来不算特别难，麻烦的是最后的结果拼接，因为每次返回的格式可能不一样，比如

```markdown
返回格式1， 以 ```json 开头

```json
[
{
//xxxx 格式
```

返回格式2，没有 ```json 开头

{
  xxx


返回格式3，从中间截断

panyName": "昂瑞微",
"companyType": "民企",
"jobLocation": "北京大连上海深圳苏州",
"recruitmentType": "秋招"
}


返回格式4， 一条数据重复返回

// ...

```

由于每次返回的结果不可控，这就给结果的拼接组装带来了一定的阻碍；因此我们需要做取舍，我的方案是每次返回的内容，先将其中能完整解析的json对象，单独拎出来；剩余解析不了的，先保证它的 json结构是完整的，然后再与后面的内容进行拼接。


JSON提取的关键实现放在了 `PartialJsonExtractor`中。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753846122807-847150f6-b4d5-4279-a95a-e8ae359f274e.png)


结果处理的关键放在了 `GatherResFormat`中，如果大家在实际使用的过程中，出现拼接结果异常的情况，可以考虑在这里进行扩展。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753846240594-fa5b606e-3dff-42c0-a46c-17343611235d.png)

## 3.输入信息的拆分
上面是按照多轮对话补全的方式来解决的返回截断问题的。


对于截断的输入信息，在excel、csv文件的输入场景下，还是挺好用的；因为这两种文件类型，我们很容易确定从哪儿截取，不会出现破坏信息完整性的情况。


那针对这两种类型的输入，我们采用输入拆分+循环调用的方式来实现。比如对于csv文件，我们在读取内容时，进行预拆分。这里的实现，假定了csv文件的第一行内容为标题。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753846486224-5fd99c6a-8a51-4b1d-91be-516d62a32275.png)

对于excel，可以借助FastExcel来实现数据提取。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753846555033-49027eb5-27fa-4a89-97d7-aeb549f6a4e4.png)

然后在使用的时候，循环调用大模型进行结果输出。


![](https://cdn.nlark.com/yuque/0/2025/png/35158118/1753846593995-f846bce8-72c8-4c9d-8a15-f65080b7bcb7.png)


从两个方案对比也很容易看出，输入信息拆分的实现，更简单，而且效果也会更好；如果有条件的话，应该选择这种方案；需要测试验证的小伙伴，可以使用 `resources/data` 下提供的测试数据进行验证。

# 三、小结
返回结果被截断的问题，可以说是整个求职派的实现过程中，遇到最麻烦的问题了。


大家在写简历的时候，一定要捋清楚这个问题的原因和解决方案，仔细看教程，并结合源码进行解读，确保自己能够和面试官进行掰扯。


那可能有小伙伴会问，如果把同步的调用方案改成异步，还会有这个截断的问题吗？


有试过的小伙伴可以评论区交流一下哦


